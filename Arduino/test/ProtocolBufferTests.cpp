#include "catch.hpp"
#include "ProtocolBuffer.hpp"

// This test writes the activations in csv file format, the data is then compared with external plotting tools.
TEST_CASE("The test factory")
{
    REQUIRE(1 == 1);
    REQUIRE(2 == 2);
    REQUIRE(6 == 6);

    ProtocolBuffer buf;
}